# Only install AutoAWQ with x86-64
autoawq; platform_machine == "x86_64" or platform_machine == "AMD64"

# Base requirements
matplotlib
streamlit
txtai[graph,pipeline-data,pipeline-llm]

# Pin grand-cypher until https://github.com/aplbrain/grand-cypher/pull/87 released
grand-cypher==0.13.0
